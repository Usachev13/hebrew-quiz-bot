# -*- coding: utf-8 -*-
"""
Курс алфавита (уровень 0).

Печатные буквы — обычный Unicode, поэтому картинки для них не нужны:
Telegram рисует их сам, и это надёжнее любого изображения (масштаб,
тёмная тема, размер шрифта у пользователя). Картинки понадобятся
отдельно для РУКОПИСНОГО начертания (כתב): в Unicode его нет, а в
Израиле от руки пишут именно так. Это следующий шаг, нужен шрифт
рукописного иврита.
"""

# (буква, название, звук, конечная форма или None)
LETTERS = [
    ("א", "алеф", "нет звука", None),
    ("ב", "бет / вет", "б / в", None),
    ("ג", "гимель", "г", None),
    ("ד", "далет", "д", None),
    ("ה", "хей", "х, в конце немая", None),
    ("ו", "вав", "в / о / у", None),
    ("ז", "заин", "з", None),
    ("ח", "хет", "х горловое", None),
    ("ט", "тет", "т", None),
    ("י", "йуд", "й / и", None),
    ("כ", "каф / хаф", "к / х", "ך"),
    ("ל", "ламед", "л", None),
    ("מ", "мем", "м", "ם"),
    ("נ", "нун", "н", "ן"),
    ("ס", "самех", "с", None),
    ("ע", "аин", "нет звука, горловая", None),
    ("פ", "пей / фей", "п / ф", "ף"),
    ("צ", "цади", "ц", "ץ"),
    ("ק", "куф", "к", None),
    ("ר", "реш", "р", None),
    ("ש", "шин / син", "ш / с", None),
    ("ת", "тав", "т", None),
]

# Буквы, которые новички путают чаще всего: различаются одной чёрточкой
# или мелкой деталью. Тренируются отдельно — по названию найти нужную.
CONFUSABLE = [
    ("ב", "בет: с «пяткой» внизу справа"),
    ("כ", "каф: закруглённая, без «пятки»"),
    ("ד", "далет: угол сверху справа, ножка короткая"),
    ("ר", "реш: закруглённый угол, без выступа"),
    ("ה", "хей: левая ножка отдельная, есть просвет сверху"),
    ("ח", "хет: сплошная перекладина, просвета нет"),
    ("ו", "вав: одна вертикальная черта"),
    ("ז", "заин: черта с шапочкой сверху"),
    ("ן", "нун софит: длинная черта, уходит вниз за строку"),
    ("ם", "мем софит: полностью закрытый квадрат"),
    ("ס", "самех: закруглённый, полностью закрытый"),
    ("ג", "гимель: две ножки, левая короче"),
    ("נ", "нун: узкая, с ножкой вправо"),
    ("ע", "аин: две ножки, сходятся в стебель"),
    ("צ", "цади: две ножки, правая примыкает сверху"),
    ("ט", "тет: закруглённая, с завитком внутри"),
    ("מ", "мем: почти закрытая, слева просвет внизу"),
]

# Буквы, у которых точка внутри (дагеш) или сбоку меняет и звук,
# и название. Для чтения это важнее, чем любые другие тонкости:
# без точки בית читается «вет», с точкой — «бет».
DOTTED = [
    ("בּ", "бет", "б"),
    ("ב", "вет", "в"),
    ("כּ", "каф", "к"),
    ("כ", "хаф", "х"),
    ("פּ", "пей", "п"),
    ("פ", "фей", "ф"),
    ("שׁ", "шин", "ш"),
    ("שׂ", "син", "с"),
]

# Огласовки (некудот). Показаны на букве ב как на подставке.
NIQQUD = [
    ("בַ", "патах", "а"),
    ("בָ", "камац", "а"),
    ("בֶ", "сеголь", "э"),
    ("בֵ", "цере", "э"),
    ("בִ", "хирик", "и"),
    ("בֹ", "холам", "о"),
    ("בֻ", "кубуц", "у"),
    ("בוּ", "шурук", "у"),
    ("בְ", "шва", "нет гласного"),
]

# Слоги для чтения: собраны из разных согласных, чтобы огласовка
# узнавалась не по одной знакомой букве.
SYLLABLES = [
    ("מָ", "ма"), ("מִ", "ми"), ("מוּ", "му"), ("מֶ", "мэ"), ("מֹ", "мо"),
    ("שָׁ", "ша"), ("שִׁ", "ши"), ("שׁוּ", "шу"), ("שֶׁ", "шэ"), ("שֹׁ", "шо"),
    ("לָ", "ла"), ("לִ", "ли"), ("לוּ", "лу"), ("לֵ", "лэ"), ("לֹ", "ло"),
    ("תָ", "та"), ("תִ", "ти"), ("תוּ", "ту"), ("תֶ", "тэ"), ("תֹ", "то"),
    ("רָ", "ра"), ("רִ", "ри"), ("רוּ", "ру"), ("רֵ", "рэ"), ("רֹ", "ро"),
    ("דָ", "да"), ("דִ", "ди"), ("דוּ", "ду"), ("דֶ", "дэ"), ("דֹ", "до"),
    ("קָ", "ка"), ("קִ", "ки"), ("קוּ", "ку"), ("קֵ", "кэ"), ("קֹ", "ко"),
    ("נָ", "на"), ("נִ", "ни"), ("נוּ", "ну"), ("נֶ", "нэ"), ("נֹ", "но"),
]

FINALS = [(base, final) for base, _, _, final in LETTERS if final]


# Курс алфавита — единственная часть приложения, где ПЕРЕВОДИТСЯ И
# ОТВЕТ. В словаре ответ всегда иврит и от языка интерфейса не зависит;
# здесь ответ — название буквы или её звук, то есть обычный текст:
# «алеф» для русского и «alef» для английского. Поэтому карточки этих
# пулов заполняют и `en` (вопрос), и `en_ans` (ответ).
#
# Звуки записаны так, как их слышит носитель языка интерфейса, а не
# по единой системе: для русского «х горловое», для английского
# «guttural kh». Международный алфавит (IPA) здесь был бы точнее и
# бесполезнее — человек на первом уроке его не читает.

from alphabet_en import (
    LETTER_NAMES_EN, LETTER_SOUNDS_EN, DOTTED_EN, NIQQUD_EN, SYLLABLES_EN,
    P_LETTER, P_SOUND, P_LOOKS, P_FINAL, P_NIQQUD, P_READ,
)
from cards import Card, letter_cid


def _en(table, key, template=None):
    """Английский текст или пустая строка, если перевода нет.

    Обращаться к таблицам через квадратные скобки нельзя: пропущенный
    перевод одной буквы уронил бы импорт этого модуля, а его тянет
    quiz.py, который тянут и бот, и приложение. Забытый перевод — повод
    для tools/check_i18n.py, а не для падения всего сервиса.

    Пустая строка при этом обязательна: собрать «what does «» look like»
    было бы хуже молчания — проверка сочла бы перевод существующим.
    """
    value = table.get(key)
    if not value:
        return ""
    return template.format(value) if template else value


def pool_names():
    """Буква -> название."""
    return [Card(f"буква {ltr}", name, "alef_names", cid=letter_cid(ltr),
                 en=P_LETTER.format(ltr), en_ans=_en(LETTER_NAMES_EN, ltr))
            for ltr, name, _, _ in LETTERS]


def pool_sounds():
    """Буква -> звук."""
    return [Card(f"звук буквы {ltr}", snd, "alef_sounds", cid=letter_cid(ltr),
                 en=P_SOUND.format(ltr), en_ans=_en(LETTER_SOUNDS_EN, ltr))
            for ltr, _, snd, _ in LETTERS]


def pool_by_name():
    """Название -> буква (обратное направление, сложнее).

    Ответ здесь — сама буква, поэтому `en_ans` не нужен: буква одна
    для всех языков. Переводится только вопрос."""
    return [Card(f"как выглядит «{name}»", ltr, "alef_by_name",
                 cid=letter_cid(ltr), en=_en(LETTER_NAMES_EN, ltr, P_LOOKS))
            for ltr, name, _, _ in LETTERS]


def pool_finals():
    """Обычная буква -> её конечная форма."""
    return [Card(f"конечная форма буквы {base}", final, "alef_finals",
                 cid=letter_cid(base), en=P_FINAL.format(base))
            for base, final in FINALS]


def pool_niqqud():
    """Огласовка -> какой звук даёт.

    Названия огласовок (патах, камац) намеренно не спрашиваем: на
    начальном уровне важно уметь прочитать знак, а не помнить, как он
    называется. Патах и камац оба дают «а» — это не ошибка данных,
    а особенность современного иврита.
    """
    return [Card(f"огласовка {shown} — какой звук", snd, "alef_niqqud",
                 cid=letter_cid(shown), en=P_NIQQUD.format(shown),
                 en_ans=_en(NIQQUD_EN, shown))
            for shown, _, snd in NIQQUD]


def pool_dotted():
    """Буква с точкой и без -> какой звук. Частая ошибка при чтении."""
    return [Card(f"звук буквы {ltr}", snd, "alef_dotted", cid=letter_cid(ltr),
                 en=P_SOUND.format(ltr), en_ans=_en(DOTTED_EN, ltr))
            for ltr, _, snd in DOTTED]


def pool_syllables():
    """Слог с огласовкой -> чтение."""
    return [Card(f"прочитай {syl}", read, "alef_syllables", cid=letter_cid(syl),
                 en=P_READ.format(syl), en_ans=_en(SYLLABLES_EN, syl))
            for syl, read in SYLLABLES]
