.env
.venv/
venv/
__pycache__/
*.pyc
.idea/
.DS_Store

# База прогресса — у каждого окружения своя, в git не хранится
progress.db
progress.db-wal
progress.db-shm

# Озвучка: ~20 МБ бинарников, генерируется скриптом на сервере
audio/

# Картинки стикерпака: исходники по 1–2 МБ, на сервере не нужны — бот
# отдаёт стикеры по file_id из Telegram, а не файлами с диска.
# Попали сюда по ошибке (git add -A), из индекса убраны.
стикеры/
